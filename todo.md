# TODO — 知識圖譜品質與升級路線

> 這份文件追蹤**圖譜品質**與**功能升級**的待辦。
> 個股報告的批次進度追蹤在 [`task.md`](task.md)，兩者不重疊。
>
> 數字皆為實測值，重跑下列指令即可更新：
> `python scripts/audit_batch.py --all` · `python scripts/build_wikilink_index.py` · `python scripts/build_network.py`

## 狀態標記

| 標記 | 意義 |
|---|---|
| `[ ]` | 未開始 |
| `[/]` | 進行中 |
| `[x]` | 已完成 |
| `[?]` | 待查 — 需要決策或外部資訊才能動工 |
| `[!]` | 阻塞 — 有依賴未滿足或與其他項目衝突 |

---

## 基準線 (2026-08)

| 指標 | 值 |
|---|---|
| 個股報告 | 1,733 份 / 98 個產業資料夾 |
| 稽核通過 | 1,718 / 1,733 |
| 實體筆記 | 6,083 |
| 報告連結解析率 (Obsidian) | 100% |
| 唯一 wikilink | 6,095 |
| 供應鏈有向圖 | 287 節點 / 1,302 條有向邊 |
| 唯一有向關係 | 14,907（supplies 11,166）|
| 檢索達成率 top-20 | 字面 100% / 換句話說 34% |
| 帶泛稱連結的報告 | 0 |

### 已完成

- [x] 修復 `discover.py --apply` 造成的巢狀連結污染（46 檔 / 12 種變體）
- [x] 合併 `utils.py` 與 `build_wikilink_index.py` 重複的分類詞表
- [x] 修復 `build_themes.py` 單一主題模式清空 `themes/README.md`
- [x] 標題統一為 `# {ticker} - [[{公司}]]`（1,614 份），修正 13 份連到名稱片段者
- [x] 10 個亂碼檔名正名（`6690_安碁資訊`、`6285_啟碁`、`8349_恒耀` 等）
- [x] 新增 `scripts/normalize_reports.py` 修復通道
- [x] 稽核新增巢狀連結硬檢查、泛稱連結警告

---

## Phase 0 — 泛稱連結清理 ✅ 完成

**問題**：點 `[[CoWoS]]` 得到 39 家可以行動；點 `[[半導體]]` 得到 136 家等於沒篩選。

**結論與原本假設不同**。逐詞量測三個指標——產業分散度、當修飾語的比例、同行是否已有具體名詞——
之後發現半數其實是台股實務在用的族群名詞，跟早已收錄的 `MLCC`、`晶圓代工` 一樣具體。
處置因此分成三類，而非一律刪除。

- [x] 產出替換建議清單並經人工確認
- [x] **升級分類**（590 次 / 239 份，不動內容）— `連接器`、`IC 設計`、`被動元件` 移入技術詞表；
      `伺服器`、`工業電腦`、`消費性電子`、`機器人`、`光通訊` 移入終端應用
- [x] **去連結**（677 次 / 228 份，保留文字只拿掉方括號）— `半導體` 296、`散熱` 91、`網通` 90、
      `自動化` 85、`記憶體` 74、`雲端服務` 38、`面板` 3
- [x] **合併**（119 次）— `儲能`→`儲能系統` 44、`[[AI]] [[伺服器]]`→`[[AI 伺服器]]` 22、
      `消費電子`→`消費性電子` 7、`液冷`→`液冷散熱` 6
- [x] **散熱細分** — `[[散熱]]模組`→`[[散熱模組]]` 等片段連結吸收 32 次；
      再標記內文既有的 `[[均熱片]]`、`[[熱管]]`。不補新事實
- [x] 規則寫進 `normalize_wikilinks`，往後每次寫入自動套用，泛稱無法再回來
- [x] 稽核 `Total tickers wikilinking a category word` 歸零

**成果**：技術詞 70→81、終端應用 14→19、泛稱 17→0。
網路圖 350→344 節點、1,478→1,315 邊——少掉的 163 條邊是 `半導體` 這類假樞紐製造的雜訊。
`[[AI 伺服器]]` 涵蓋 188 家（原 179）。

### 後續（本階段暴露出來的）

- [ ] **15 份報告內容過薄**，去連結後低於 8 個 wikilink 門檻。
      這不是誤判——例如 6218 豪勉的報告裡 Juniper 是純文字從未被連結，先前靠泛稱湊滿門檻。
      按當初決定，補具體名詞留給日後 enrichment：
      `3684 榮昌 · 4540 全球傳動 · 5212 凌網 · 5443 均豪 · 6103 合邦 · 6109 亞元 · 6113 亞矽 ·
      6196 帆宣 · 6206 飛捷 · 6218 豪勉 · 6226 光鼎 · 6259 百徽 · 6277 宏正 · 6423 億而得 · 6509 聚和`
- [ ] `散熱` 仍可再細分：`液冷板`、`3D VC`、`均溫板` 等詞在內文出現但尚未成為節點

## Phase 1 (B) — 讓資料在 Obsidian 裡真的能點 ✅ 完成

**原本的設想是錯的。** 計畫用 frontmatter `aliases` 讓 `[[台積電]]` 指向 `2330_台積電.md`，
查證後發現**原生 Obsidian 的裸 `[[別名]]` 不會解析**——別名只出現在自動完成選單。
社群外掛 Alias Linker 與 obsidian-auto-linker 存在的理由正是補上這件事。

改用只依賴 Obsidian 最基本保證行為的方案：**檔名解析**。

- [x] 落點：`entities/{名稱}.md`，位於 `Pilot_Reports/` 之外，
      所有報告掃描器都要求 `NNNN_` 開頭檔名，因此不會誤讀
- [x] 產生 6,083 則實體筆記，涵蓋全部 wikilink（非只有出現 2 次以上者）
- [x] 每則含 `type`／`mentions`／`reports`，有對應報告的公司另含 `id`（股票代號）
      與通往報告的連結；並列出最常一起出現的實體
- [x] 1,733 份報告加上 frontmatter：`ticker`／`company`／`sector`／`industry`／
      `market_cap`／`enterprise_value`
- [x] `utils.update_metadata` 同時寫入內文與 frontmatter，兩者無法走鐘
- [x] `scripts/build_obsidian_vault.py` 與使用指南 `obsidian.md`（含 Dataview 範例）
- [x] 驗證：全 vault 7,846 個 `.md`、41,062 次連結出現位置全面掃描（非抽樣）

**成果**：報告內的連結解析率 **100%**。整個 vault 只剩 11 處無法解析。

### 順手修掉的

- [x] 3 個連結是 Obsidian 管線語法 `[[2330_台積電|台積電]]`，被當成名為
      `2330_台積電|台積電` 的壞節點（7 處）。`unpipe_wikilinks` 已納入正規化
- [x] 4 個「兩個實體寫成一個」的複合連結已拆開
      （`[[CPU/晶片組]]` → `[[CPU]]/[[晶片組]]` 等）

### 已知限制

- 5 個名稱本身帶斜線的正當術語無法作為檔名：`DC/DC`、`Low Dk/Df`、
  `PIC/S GDP`、`PIC/S GMP`、`Super I/O`
- `SOC`（資安維運中心）與 `SoC`（系統單晶片）在不分大小寫的檔案系統上會撞檔，
  保留較常用的 `SoC`

## Phase 2 (A) — 邊帶方向與型別 ✅ 完成

**沒有改寫報告格式**。原本規劃「讓上下游成為結構化資料而非排版慣例」，
但方向資訊本來就完整地寫在報告裡——`**上游:**`／`**下游:**`／`### 主要客戶`／
`### 主要供應商`。需要的是一個解析器，不是重寫 1,733 份報告。

- [x] `scripts/relations.py`：把排版慣例解析為有向型別邊
- [x] 關係型別：`supplies`（A 供應給 B，有向）、`competes`、`mentions`
- [x] `build_network.py` 改為有向圖：箭頭、型別著色、型別開關、節點數滑桿
- [x] `build_themes.py` 移除「往前找 100 字元」的角色猜測，改讀關係
- [x] 實體筆記加入「上游 — 供應給它」「下游 — 它供應給」
- [x] D3 改為 vendored（`network/vendor/`），頁面離線可用
- [x] 瀏覽器實測：渲染、箭頭、tooltip、型別過濾、節點滑桿，零 console error

**成果**：11,166 條 supplies、62 條 competes、3,733 條 mentions，
共 14,907 條唯一有向邊。預設視圖 287 節點／1,302 條有向邊。

### 未採用的原項目與理由

- **全面實體 ID**：台股公司已用股票代號當 `id`（實體筆記 frontmatter）。
  替其餘 4,300 個技術／材料／外商實體鑄造合成 ID 沒有實際收益——
  正規化後的名稱本身就是穩定鍵，而 `SOC`／`SoC` 這類問題是靠保持區分解決的，
  不是靠 ID。等有外部識別碼（如 LEI、統編）可對接時再說。
- **`WIKILINK_ALIASES` 改為 alias → ID 對照表**：同上，目前 alias → canonical name
  已足夠，改成 ID 只是多一層轉換。

### 順手修掉的

- [x] `network/index.html` 原本從 CDN 載入 D3，離線完全不會渲染，
      但 README 稱它 self-contained。已 vendored（D3 7.9.0，ISC）
- [x] 舊主題頁的上下游分類近乎隨機。以 CoWoS 為例，舊版「上游」列的是
      大量／友威科／高鋒等；新版依關係得出欣興（載板）、辛耘／弘塑／由田（設備）、
      穎崴（測試）、亞翔／洋基工程（廠務），共 27 家

## Phase 3 (C) — 主題檢索 / 供應鏈擴展 / 財務篩選 ✅ 完成（語意索引待決策）

`scripts/screen.py` 把前面所有成果串成一條查詢流程：
**主題檢索 → 沿有向邊擴展供應鏈 → 用財務表篩選**。

- [x] 檢索：BM25 over 字元 bigram（中文沒有詞界，這是零依賴的正確做法）
- [x] 供應鏈擴展：`--hops N --direction up|down|both`，每筆結果標明經由誰連過來
- [x] 財務篩選：`--pe`、`--gross-margin`、`--market-cap` 等 10 個欄位，
      支援 `>30`、`<=20`、`10-25`
- [x] `utils.parse_financials` 解析估值表與年度表（估值表覆蓋 1,729/1,733）
- [x] `scripts/eval_screen.py` 評測：18 題問答集，以資料庫自身的 wikilink 為標準答案

**實測數字（top-20）**

| 查詢類型 | 達成率 | 精確率 |
|---|---|---|
| 字面相符（如「CoWoS」→ CoWoS） | **100%** | 82% |
| 換句話說（如「水冷」→ 液冷散熱） | **34%** | 27% |

達成率 = 命中 ÷ min(k, 應命中)，已扣除 top-k 造成的上限。

### 待決策：要不要加語意索引

上表的 **66 個百分點落差就是 embedding 能補、字面檢索補不了的部分**。
「水冷」與「液冷散熱」沒有共同 bigram，達成率 0%。

加了會得到什麼：換句話說的查詢從 34% 拉高。
代價：`sentence-transformers` + `torch` 約 500MB 依賴，而本專案目前只依賴
`yfinance`／`pandas`／`tabulate`，README 也強調 Python 腳本「免 token、純本地」。

替代方案（較輕）：手工維護一份查詢同義詞表，涵蓋前 30 個主題概念。
效果有限但零依賴，且可用 `eval_screen.py` 直接量測增益。

**尚未決定，需要人拍板。**

### 未採用的原項目與理由

- **向量索引**：見上，改為待決策項目而非直接實作。
  先把零依賴能做好的部分做完並量測，讓依賴決策有數字支撐。

## 待查問題

- [?] **`-KY` / `-創` 後綴要不要進節點名稱**
  106 份報告檔名帶後綴。目前標題照檔名產生 `[[臻鼎-KY]]`，但其他報告寫 `[[臻鼎]]`，
  靠別名表併成一個節點。已知只有 `臻鼎` 一組真的出現雙寫（16 次 vs 13 次），
  其餘 105 個兩種寫法在庫內都是 0 次。
  Phase 2 導入實體 ID 後此問題自動消失，屆時可移除該別名。

- [?] **72% 的 wikilink 只出現在 1 份報告**（6,099 個裡有 4,390 個）
  這些節點對圖譜沒有貢獻，是「事實」不是「連結」。
  需要決定：視為正常長尾保留、或是研究時該優先連到已存在的節點。
  建議 Phase 1 做完、能用眼睛看圖之後再判斷。

- [?] **中文長短名同義變體尚未合併**
  實體筆記讓這類重複浮現：`聯電`(69) 與 `聯華電子`(27)、`中油`(18) 與 `台灣中油`(47)、
  `環球晶`(8) 與 `環球晶圓`(2) 都是同一家公司的兩個節點。
  **但不可用前綴比對自動合併**——實測 398 組候選中大量是致命誤判：
  `聯發`(1459 紡織) 會被錯併成 `聯發科`(2454)、`中華`(2204 中華汽車) 會吃掉
  `中華電信`(2412)、`南亞`(1303) 會吃掉 `南亞電路板`(8046)。
  這正是 CLAUDE.md 規則 2 所稱「最具破壞性的錯誤」，必須逐案以股票代號驗證，
  適合等 Phase 2 的實體 ID 就位後再處理。

- [?] **`discover.py --apply` 在中文沒有可靠詞界，套用前必須人工看過**
  實例：搜尋 `熱管` 命中 6 家，其中 3 家是誤中——4930 燦星網與 4609 唐鋒的「**加**熱管」是家電
  加熱元件，3325 旭品的「散**熱管**」是另一個詞。三者剛好都在 Electronic Components 之外，
  因此用 `--sector` 限定後才套用。邊界守衛只擋得住拉丁字元（擋 CJK 會讓「採用液冷散熱」
  完全比對不到），所以 `--apply` 永遠是需要人工複核的動作，不是自動化步驟。

- [?] **`build_wikilink_index.py` 與 `build_network.py` 的計數基準不同**
  前者算「出現次數」，後者算「出現在幾份報告」。目前沒有錯，但對外呈現數字時容易混淆。
  Phase 2 統一資料層時一併收斂。

---

## 已知依賴與衝突

| 項目 | 說明 |
|---|---|
| Phase 1 → Phase 2 | 已處理：實體筆記帶 `id:` 欄位（目前只有本庫有報告的台股公司填了代號），Phase 2 可直接沿用為實體鍵，不需靠檔名。 |
| Phase 0 → Phase 1 | 非硬性依賴，但在 Obsidian 裡點到 `[[半導體]]` 跳出 136 家的挫折感，比在終端機看到數字強烈得多。建議先清掉 `半導體`、`伺服器` 兩個最大的。 |
| Phase 2 → Phase 3 | 已滿足：`relations.py` 提供有向型別邊，GraphRAG 可以沿供應鏈做多階擴展。 |
| 全域 | **財務區塊 (`## 財務概況`) 一律不得改動**。所有腳本都以此為分界線，任何新工具都必須遵守。 |
| 全域 | 新增檔案若放進 `Pilot_Reports/`，檔名必須是 `{4碼}_{名稱}.md` 才會被腳本掃到；不符者會被靜默忽略。實體筆記因此建議放在 `entities/`。 |
